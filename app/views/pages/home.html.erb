<h1 class="text-2xl font-bold mb-4">Fragment Caching Demo with Solid Cache</h1>

<p>
  Reload this page to see that the “expensive” section only actually renders once,
  then loads instantly on subsequent requests.
</p>

<% cache("expensive_block") do %>
  <% sleep 2 %> <!-- Artificially slow down to simulate expensive work -->
  <div class="mt-4 p-4 bg-gray-100 border rounded">
    <p>
      <strong>Expensive block rendered at:</strong> <%= Time.current %>
    </p>
    <p class="text-sm text-gray-600">
      We paused 2 seconds before rendering this the first time.
    </p>
  </div>
<% end %>

